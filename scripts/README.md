you can define package name(-p), project name(-n), kotlin version(-v),
and the main class name (-m) by using following command from project root fold.

>$your_project_root$>bash ./scripts/rename.sh -p "com.demo.dts" -n "kotlin-mvn-template" -k "1.4.0" -m "StartUp"

>$your_project_root$>$mvn install